# genaihindi

```
    pip install ipykernel
 ```   
```
     conda create -p env python=3.8 -y

     source activate ./env

     pip list

     git status

     ls -a

     conda install -p /config/workspace/env ipykernel --update-deps --force-reinstall

     git add .

     git commit -m "folder structure updated"

     git config --global user.email "nitishchandra1206@gmail.com"

     git config --global user.name "chillbroooo"

     git commit -m "folder structure updated"

     git remote add origin https://github.com/chillbroooo/genaihindi.git

     git push -f origin main

     pip install -r requirements.txt

     conda activate /config/workspace/env
```